{
  "mcpServers": {
    "invisible-playwright": {
      "command": "bash",
      "args": ["scripts/invisible-playwright-mcp.sh"],
      "timeout": 120000,
      "env": {
        "IP_HEADLESS": "true",
        "IP_LOCALE": "de-DE",
        "IP_TIMEZONE": "Europe/Berlin",
        "IP_STORAGE_STATE": "tmp/browser-state.json"
      }
    }
  }
}
